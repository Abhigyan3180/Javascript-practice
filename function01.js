function callMe(username="Ansh"){
    if(!username){
        console.log(`Invalid username`)
        return
        }
        console.log(`${username} is login to the device`);
        
    }

    callMe("Abhigyan");